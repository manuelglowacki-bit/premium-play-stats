/// <reference types="vite/client" />

/** Identifiant du build, injecte par vite.config.ts. Vaut la meme valeur que
 * le champ `version` de /version.json publie a cote du bundle. */
declare const __BUILD_ID__: string;
